pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'mvn --debug -B -DskipTests clean package'
            }
        }
        stage('Test') {
            steps {
                sh 'mvn --debug test'
            }
            post {
                always {
                    junit 'target/surefire-reports/*.xml'
                }
            }
        }
        stage('Deliver') {
            steps {
                sh './jenkins/scripts/deliver.sh'
            }
        }
    }
}
